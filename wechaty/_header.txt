/* 栖语 Wechaty 微信网关
 * Python 后端通过 HTTP 桥接：
 *   GET  /status   -> {state, qr, user, message}
 *   POST /send     -> {to, text, image} 单条发送
 *   POST /stop     -> 退出登录并退出进程
 * 入站消息 -> POST <QIYU_CALLBACK_URL>  {from, text, images[], type, room, ts, id}
 * 环境变量:
 *   QIYU_GATEWAY_PORT        网关端口(默认18765)
 *   QIYU_CALLBACK_URL        Python 回调地址
 *   QIYU_CALLBACK_SECRET     回调校验密钥
 *   WECHATY_PUPPET           wechaty-puppet-wechat4u(默认) / wechaty-puppet-service
 *   WECHATY_PUPPET_SERVICE_TOKEN  服务型 puppet 的 token
 */
