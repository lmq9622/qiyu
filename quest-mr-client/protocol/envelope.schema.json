{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "qiyu.quest.protocol.v1.envelope",
  "title": "Qiyu Quest Envelope",
  "description": "Quest 与 Qiyu Gateway 每条 WebSocket 消息的统一封装。",
  "type": "object",
  "required": ["v", "id", "type", "ts", "payload"],
  "additionalProperties": false,
  "properties": {
    "v": {
      "const": "1.0.0",
      "description": "协议版本"
    },
    "id": {
      "type": "string",
      "minLength": 1
    },
    "type": {
      "type": "string",
      "pattern": "^[a-z0-9]+\\.[a-z0-9_]+$"
    },
    "ts": {
      "type": "integer",
      "minimum": 0,
      "description": "Unix epoch 毫秒"
    },
    "session": {
      "type": "string",
      "description": "hello_ack 后必须携带；client.hello 可不带"
    },
    "reply_to": {
      "type": "string"
    },
    "seq": {
      "type": "integer",
      "minimum": 0,
      "description": "会话内单调递增序号；重复/乱序事件不重复执行"
    },
    "ack": {
      "type": "integer",
      "minimum": 0,
      "description": "已确认的对端最大连续序号"
    },
    "payload": {
      "type": "object"
    }
  }
}
