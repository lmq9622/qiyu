# 栖语 v0.0.17 · 发布说明

**发布日期**：2026-09-12（对应 2026-09-01 打包的历史版本）
**版本线**：`0.0.x`（历史 demo 线最后一版）

---

## 一、这一版是什么

「栖语」是一个 Windows 桌面 AI 伴侣应用：本地可跑、可自建、聊天记录与记忆留在自己机器上。
v0.0.17 是 `0.0.x` 线的最后一版，这里发布的是**可运行的历史版本**，作为归档，不再跟进后续开发。

主要能力：角色工坊、真人感聊天链路（意图路由 / 话题情绪状态 / 记忆注入 / 回复分条）、
长期记忆与记忆面板、RAG 知识库、联网工具、主动消息、微信外部通讯（ClawBot / Wechaty / itchat / 本机接管）。

完整功能与限制见 [`README.md`](README.md)；使用步骤见 [`docs/用户手册.md`](docs/用户手册.md)。

---

## 二、下载与校验

| 文件 | 大小 | SHA256 |
|---|---:|---|
| `Qiyu-0.0.17-win64.exe` | 约 137.9 MB | `1DA6DD9D26E46EE00BF9C8BA8A414BB7CD420E6723E9E34FE0EA1D0D93CF4FB7` |

双击即可运行（会自带浏览器内核窗口，无需额外安装）。Windows SmartScreen 可能提示未签名，
选择「更多信息 → 仍要运行」。运行数据落在 `%USERPROFILE%\.ai_companion\`。

**注意**：本版**不内置模型权重**，第一次使用需要在「设置 → LLM 设置」里填入你自己的
OpenAI 兼容服务（本机 llama.cpp / Ollama 网关，或任一云端 API）。

如果 8765 端口被别的程序占用，可用环境变量换端口：

```bat
set QIYU_PORT=8888
Qiyu-0.0.17-win64.exe
```

---

## 三、本版内容

- 桌面客户端（PyInstaller 单文件 exe）+ FastAPI 后端，双击拉起，无需命令行
- 角色工坊：人设 / 用户设定 / 记忆提示 / 语气 / 标签 / 头像，支持流式「AI 生成人设」
- 聊天：意图路由（直答 / 思考 / 联网 / 工具）、分条回复、话题与情绪状态
- 记忆：分层记忆（用户档案 / 长期记忆 / 每日摘要）+ 标签管理 + 记忆面板
- 知识库：`txt / md / pdf / docx / doc` 自动切块 + 本地 bge-small-zh 向量检索
- 联网：搜索 / 打开链接 / 热点 / 商品 / 视频信息 / 图片，真实执行后再回复，失败如实说明
- 主动消息：09:00–22:00 按好感度随机主动发起
- 外部通讯：ClawBot、Wechaty、itchat、本机微信自动化（多账号、联系人→角色映射）
- 9 个 OpenAI 兼容预设（DeepSeek / GLM / MiniMax / OpenAI / Claude / Gemini / Grok / Kimi / 小米 MiMo）
- 项目日志（截止 0.0.x）：`PROJECT_LOG.md`

---

## 四、防反编译加固（讲清楚它做了什么、没做什么）

Release 里的 exe 在常规打包之外加了一层防护：

1. **包内没有明文源码**：打包参数不再把 `.py` 以数据文件形式塞进包里；
2. **应用自身模块以加密字节码分发**：`demo.py`、`channels/`、`memory/`、`rag/`、`tools/` 等模块
   在构建期编译成字节码并加密，随包以 `.pye` 形式存在，运行时由加载器在内存中解密执行；
3. **静态分析看不到模块清单**：包内的模块表中不再出现这些模块，
   直接用解包工具得到的只是密文，无法像通常那样丢进反编译器拿到源码；
4. 打包时启用字节码优化，去掉文档字符串等易读信息。

**必须说清楚的**：这是「提高逆向门槛」的混淆方案，**不是密码学强度保护**。
解密所需的密钥随程序一起分发，有决心和时间的人仍然可以还原出代码。
它的目标是挡住随手解包、复制、改名再发布的低成本破解，而不是宣称不可破解。
该加固工具不随本仓库发布，因此本仓库源码的 `build.py` 产出的是未加固版本。

---

## 五、与原始 v0.0.17 的差异（如实记录）

| 项 | 说明 |
|---|---|
| `tools/` 模块 | 原始快照的 `.gitignore` 排除了整个 `tools/` 目录，`tools/web.py` 从未入库；本仓库用 **2026-08-31 的同期备份**补回该模块，函数与 `demo.py` 调用一致 |
| 客户端端口 | `client.py` 改为读取 `QIYU_PORT`（默认 8765），否则端口被占用时 exe 无法启动 |
| 脱敏 | 开发期内网地址 `192.168.2.6` 统一替换为 `127.0.0.1`；移除含本机绝对路径的开发期补丁脚本 |
| 文档 | 新增本发布说明与 `docs/用户手册.md`；`PROJECT_LOG.md` 只保留到 0.0.x 段 |

---

## 六、已知限制

- 需自备模型后端；不内置权重。
- Letta / PostgreSQL 需自行安装；本地 Embedding 首次使用会联网下载模型。
- 微信通道存在账号被限制的风险（itchat 已停维护、本机接管依赖微信 UI 自动化），请用小号自测。
- 「真人感」结论来自自建回测（12 人设 × 场景 × 多轮），不是公开基准。
- 原始日志中记录的 Phase B 全量 21600 次回测**未跑完**，本仓库没有把它写成通过。

## 七、许可证

MIT，详见 [`LICENSE`](LICENSE)。